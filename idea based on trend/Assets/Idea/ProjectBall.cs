using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProjectBall : MonoBehaviour
{
    public Transform _lugar1;
    public Transform _lugar2;
    public GameObject _place;


public void trajeto(){

    //pego a distancia inicial
  Vector3 ini;
  ini = _lugar1.position;
    //pego a distancia final
 Vector3 fim;
  fim = _lugar2.position;
    //crio a cada x um objeto no trajeto z entre distancia inicial
    
    //final
}
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
